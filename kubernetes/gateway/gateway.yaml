apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: hivebox
  namespace: envoy-gateway-system
spec:
  gatewayClassName: hivebox
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
